import RecipeList from '@/components/recipes/RecipeList';

export const metadata = {
  title: 'Recipes | GlobalBites',
  description: 'Discover delicious recipes from around the world',
};

export default function RecipesPage() {
  return <RecipeList />;
}
